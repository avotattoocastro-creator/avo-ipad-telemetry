workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Get packages
        script: flutter packages pub get
      - name: Build iOS
        script: flutter build ios --release --no-codesign

